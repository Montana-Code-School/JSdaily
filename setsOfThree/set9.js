/************************
  Exercise 1:
  A trimorphic number is a number whose cube ends in the number
  itself.  Write a function that returns true if the number is
  trimorphic and false if it is not.
************************/

const triMorph = (num) => {
  
}

/***********************
  Exercise 2:
  A binary search tree is a container that holds items in memory, it is made of
  nodes.  Each node stores a key and a possible a value and has a binary fork
  one to left and one to the right and connects to other nodes.
***********************/
const binarySearchTree = (tree) => {

}

/***********************
  Exercise 3:
  Vigenere cipher takes in an input and a key and uses the key to build
  a shifted string, encrypting a message that can only be decrypted if one knows
  the key.  https://en.wikipedia.org/wiki/Vigen%C3%A8re_cipher
***********************/

const vigenereCipher = (input, key) => {

};

module.exports = {
  triMorph,
  binarySearchTree,
  vigenereCipher
}
